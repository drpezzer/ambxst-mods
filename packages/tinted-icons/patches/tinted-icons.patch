diff --git a/modules/bar/systray/SysTrayItem.qml b/modules/bar/systray/SysTrayItem.qml
index 0b725b1a..7e46fd6a 100644
--- a/modules/bar/systray/SysTrayItem.qml
+++ b/modules/bar/systray/SysTrayItem.qml
@@ -169,6 +169,7 @@ MouseArea {
     Tinted {
         sourceItem: trayIcon
         anchors.fill: trayIcon
+        monochrome: true
     }
 
     StyledToolTip {
diff --git a/modules/bar/workspaces/Workspaces.qml b/modules/bar/workspaces/Workspaces.qml
index 48f68cdd..8a6acada 100644
--- a/modules/bar/workspaces/Workspaces.qml
+++ b/modules/bar/workspaces/Workspaces.qml
@@ -528,6 +528,8 @@ Item {
                         Tinted {
                             sourceItem: mainAppIcon
                             anchors.fill: mainAppIcon
+                            monochrome: true
+                            tintColor: ((monitor && monitor.activeWorkspace ? monitor.activeWorkspace.id : undefined) == button.workspaceValue) ? Styling.srItem("primary") : Styling.srItem("overprimary")
                         }
                     }
                 }
@@ -665,6 +667,8 @@ Item {
                         Tinted {
                             sourceItem: mainAppIconVert
                             anchors.fill: mainAppIconVert
+                            monochrome: true
+                            tintColor: ((monitor && monitor.activeWorkspace ? monitor.activeWorkspace.id : undefined) == buttonVert.workspaceValue) ? Styling.srItem("primary") : Styling.srItem("overprimary")
                         }
                     }
                 }
diff --git a/modules/components/Tinted.qml b/modules/components/Tinted.qml
index 7144741f..5dee00ac 100644
--- a/modules/components/Tinted.qml
+++ b/modules/components/Tinted.qml
@@ -8,6 +8,13 @@ Item {
     id: root
     property var sourceItem: null  // The icon item to tint
     property bool fullTint: false  // If true, apply solid primary color instead of shader
+    // Monochrome tint: colourise the icon in a single hue while keeping its own
+    // shading, so glyph detail survives. Replaces the palette shader for app
+    // icons. Unlike fullTint it does not flatten the icon to a silhouette first,
+    // which is why fullTint stays untouched for the launcher logo: a dark logo
+    // colourised at its own brightness reads as black.
+    property bool monochrome: false
+    property color tintColor: Styling.srItem("overprimary")
 
     // Subset of colors for optimization (consistent with TintedWallpaper.qml)
     readonly property var optimizedPalette: [
@@ -84,19 +91,20 @@ Item {
                 function onVisibleChanged() { if (root.visible) internalSource.scheduleUpdate(); }
             }
 
-            // Full tint fallback (solid color)
+            // Full tint fallback (solid color), or monochrome tint keeping the
+            // icon's own brightness
             MultiEffect {
-                visible: root.fullTint
+                visible: root.fullTint || root.monochrome
                 anchors.fill: parent
                 source: internalSource
-                brightness: 1.0
+                brightness: root.fullTint ? 1.0 : 0.0
                 colorization: 1.0
-                colorizationColor: Styling.srItem("overprimary")
+                colorizationColor: root.tintColor
             }
 
             // Shader-based tint
             ShaderEffect {
-                visible: !root.fullTint
+                visible: !root.fullTint && !root.monochrome
                 anchors.fill: parent
                 
                 property var source: internalSource
diff --git a/modules/dock/DockAppButton.qml b/modules/dock/DockAppButton.qml
index 3132417b..fdca9264 100644
--- a/modules/dock/DockAppButton.qml
+++ b/modules/dock/DockAppButton.qml
@@ -110,6 +110,7 @@ Button {
                     Tinted {
                         sourceItem: appIcon
                         anchors.fill: appIcon
+                        monochrome: true
                     }
                 }
 
diff --git a/modules/widgets/launcher/LauncherView.qml b/modules/widgets/launcher/LauncherView.qml
index 9af6e933..07ba94f8 100644
--- a/modules/widgets/launcher/LauncherView.qml
+++ b/modules/widgets/launcher/LauncherView.qml
@@ -751,6 +751,16 @@ Rectangle {
                             Tinted {
                                 anchors.fill: parent
                                 sourceItem: appIconImage
+                                monochrome: true
+                                tintColor: {
+                                    if (isExpanded) {
+                                        return Styling.srItem("pane");
+                                    } else if (appLauncher.selectedIndex === index) {
+                                        return Styling.srItem("primary");
+                                    } else {
+                                        return Styling.srItem("overprimary");
+                                    }
+                                }
                             }
                         }
 
